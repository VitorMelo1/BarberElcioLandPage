# Dev: deixe vazio/apagado — o .env.development já aponta pra /api (proxy do Vite).
# Produção: o .env.production usa /api (nginx same-origin). Só sobrescreva se a API
# for servida em OUTRO domínio (não recomendado — quebra o cookie httpOnly):
# VITE_API_URL=https://api.seu-dominio.com.br/api
