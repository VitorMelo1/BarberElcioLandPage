# /public/images

- `hero-poster.jpg` — frame estático da hero (gerado do vídeo).
- `barber.jpg` — foto do Elcio trabalhando (cena "O Bruxo").
- `mandala.svg` / `logo.svg` — logo e mandala isolada (máscara da hero + motivo).
- `portfolio/` — fotos do portfólio (referenciadas em `src/data/gallery.ts`).
- `mobile/` — sequência/poster da tesoura p/ o fallback sem WebGL no celular.
